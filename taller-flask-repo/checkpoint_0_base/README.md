# Checkpoint 0 — Base

Punto de partida del taller. El entorno está listo, solo hace falta activarlo y arrancar.

## Pasos

```bash
python -m venv venv
venv\Scripts\activate        # Windows
pip install -r requirements.txt
python app.py
```

El servidor arranca en http://localhost:5000  
Por ahora no tiene rutas — las vas a crear en el Bloque 1.
